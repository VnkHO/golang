/*


	func newDeckFromFile(filename string) deck {
		byteSlice, err := ioutil.ReadFile(filename)
		if err != nil {
			fmt.Println("Error :", err)
			os.Exit(1)
		}

	}

	byteSlice err := ioutil.ReadFile(filename)

	err -> Value of type 'error'.
	If nothing went wrong, it will have a value of 'nil'


*/