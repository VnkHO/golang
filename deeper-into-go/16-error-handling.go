/*

	func newDeckFromFile(filename string) deck {
		byteSlice, err := ioutil.ReadFile(filename)
		if err != nil {
			fmt.Println("Error :", err)
			os.Exit(1)
		}
		sliceString := strings.Split(string(byteSlice), ",") // Convert Byte to string
		return deck(sliceString)
	}


*/